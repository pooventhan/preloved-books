<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title }}</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.14.0/css/all.min.css" rel="stylesheet"</script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
</head>
<body class="p-3">
  <div class="container">
    <p>{{intro}}</p>
    <p>{{intro2}}</p>
    
    <a class="btn btn-primary btn-sm" href="mailto:{{email}}" role="button"><i class="fa fa-envelope"></i> Email me!</a>
    <br/><br/>
    <div>
      <strong>Note:</strong>
      <p>Books marked <i class="fas fa-pen-square"></i> have been written / highlighted.</p>
    </div>
  </div>
  <div class="container border">
    
    {{#if books}}
      <div class="row mt-2">
          {{#each books}}
            <div class="col mb-3">
              <div>
                <img class="rounded" style="position:relative" src="{{this.cover}}" width="130" data-bs-toggle="modal" data-bs-target="#modal{{this.index}}">
                {{#if this.sold}}
                  <span class="position-absolute top start translate-middle badge rounded-pill bg-danger">
                    SOLD
                  </span>
                {{/if}}
              </div>
              {{#if this.written}}
                <p><i class="fas fa-pen-square"></i></p>
              {{/if}}
              <!-- Modal -->
              <div class="modal fade" id="modal{{this.index}}" tabindex="-1" aria-labelledby="modalLabel{{this.index}}" aria-hidden="true">
                <div class="modal-dialog">
                  <div class="modal-content">
                    <div class="modal-header">
                      <h5 class="modal-title" id="modalLabel{{this.index}}">{{this.title}}</h5>
                      <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                    </div>
                    <div class="modal-body">
                      {{{ this.description }}}
                    </div>
                  </div>
                </div>
              </div>

              <div class="row mt-2 ">
                <span class="fw-bold">{{this.title}}</span>
                <span class="fst-italic">{{this.author}}</span>
              </div>
              <div>
                <span class="text-primary fw-bold">RM{{{ discount this.originalPrice this.discount }}}</span>
                <span class="fw-light"><del>RM{{{ twoDecimalPoints this.originalPrice }}}</del></span>
              </div>
            </div>
          {{/each}}
      </div>
    {{/if}}
  </div>
</body>
</html>